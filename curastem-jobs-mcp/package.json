{
  "name": "curastem-jobs-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Curastem Jobs MCP server — agent-first wrapper around the Jobs API for Gemini tool-calling",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.71.0"
  }
}
